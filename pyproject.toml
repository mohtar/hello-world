[tool.poetry]
name = "hello-world"
version = "0.1.0"
description = ""
authors = ["Mohd Tarmizi"]

[tool.poetry.dependencies]
python = "^3.8"
aiohttp = "^3.6.2"
docopt = "^0.6.2"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
